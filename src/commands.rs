use super::config::Config;

pub fn insert(config: Config) {
    println!("{:?}", config);
}

pub fn view(config: Config) {
    println!("{:?}", config);
}

pub fn list(config: Config) {
    println!("{:?}", config);
}
